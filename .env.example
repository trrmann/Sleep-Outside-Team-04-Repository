# Example environment variables
# Copy this to src/.env and customize for your environment

# Backend API URL
# Development: /api/ (uses Vite proxy)
# Production: https://wdd330-backend.onrender.com/
VITE_SERVER_URL=/api/

# Employee Configuration
# Add employee emails to src/js/employeeConfig.mjs to grant admin access
# This is a fallback if the backend doesn't include employee status in JWT tokens
