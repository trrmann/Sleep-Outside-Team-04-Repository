# Production environment variables (used by `npm run build`)
# Keep secrets out of this file; set them in Render or use `src/.env.production.local` for local testing.
VITE_SERVER_URL=https://wdd330-backend.onrender.com/
# Production environment - uses proxy server to avoid CORS
# The Node.js server will proxy /api/* to the backend
VITE_SERVER_URL=/api/
